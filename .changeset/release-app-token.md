---
"calibre-babelio-plugin": patch
---

Use a GitHub App token in the release workflow so the changesets "Version Packages" PR triggers CI and can satisfy the required `build-and-test` status check before merging.
